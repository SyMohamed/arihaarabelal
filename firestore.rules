rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to everyone (public site)
    // Allow write access to everyone (admin auth handled at app level)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
